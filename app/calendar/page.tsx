"use client";
import { useTradingStore } from "@/store/use-trading-store";
export default function Page(){const trades=useTradingStore(s=>s.trades);const byDay=trades.reduce<Record<string,{pnl:number,count:number}>>((a,t)=>{a[t.startDate]??={pnl:0,count:0};a[t.startDate].pnl+=t.pnl;a[t.startDate].count++;return a;},{});return <div className="glass rounded-2xl p-6"><h2 className="text-xl font-semibold mb-4">Calendar Heatmap</h2><div className="grid md:grid-cols-4 gap-3">{Object.entries(byDay).map(([day,v])=><div key={day} className={`rounded-xl border p-3 ${v.pnl>=0?"border-emerald-500/40 bg-emerald-500/10":"border-rose-500/40 bg-rose-500/10"}`}><p className="text-sm text-slate-300">{day}</p><p className="text-lg font-semibold">{v.pnl.toFixed(2)}</p><p className="text-xs text-slate-400">{v.count} trades</p></div>)}</div></div>}
